spring:
  datasource:
    url: jdbc:h2:mem:test # 테스트를 위한 인메모리 DB 사용
    username: sa
    password:
    driver-class-name: org.h2.Driver


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug #jpa? sql? ??? ??? ????.
    org.hibernate.type: trace # ?? ????? ?? ????? ??
